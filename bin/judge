#!/usr/bin/env php
<?php
$baseDir = dirname(__DIR__);
require_once "{$baseDir}/src/helpers.php";

$language = $argv[1];
$hole = $argv[2];
$script = $argv[3];

exit((int)!judge($language, $hole, $script));
